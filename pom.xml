<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.github.raju2210</groupId>
	<artifactId>toolbox-container</artifactId>
	<version>${revision}</version>
	<name>demo</name>
	<description>Container image containing bunch of dev tools for
		debugging applications</description>

	<properties>
		<revision>1.0.0-SNAPSHOT</revision>
		<java.version>1.8</java.version>
		<jkube.version>1.1.1</jkube.version>
		<m2e.plugin.version>1.0.0</m2e.plugin.version>
		<jkube.build.source.dir>src/main/container</jkube.build.source.dir>
	</properties>

	<scm>
		<connection>scm:git:${project.scm.url}</connection>
		<developerConnection>scm:git:${project.scm.url}</developerConnection>
		<url>git@github.com:rajesh-kumar/toolbox-container.git</url>
		<tag>HEAD</tag>
	</scm>

	<distributionManagement>
		<repository>
			<id>github</id>
			<name>GitHub Apache Maven Packages</name>
			<url>https://maven.pkg.github.com/rajesh-kumar/toolbox-container</url>
		</repository>
	</distributionManagement>

	<build>
		<resources>
			<resource>
				<directory>src/main/container</directory>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.eclipse.jkube</groupId>
				<artifactId>kubernetes-maven-plugin</artifactId>
				<version>${jkube.version}</version>
				<configuration combine.children="append">
					<images>
						<image>
							<name>%g/toolbox:%l</name>
							<build>
								<contextDir>${project.basedir}/src/main/container</contextDir>
								<dockerFile>Dockerfile</dockerFile>
								<filter>@</filter>
								<assembly>
									<name>maven</name>
									<targetDir>/maven</targetDir>
									<inline>
										<id>toolbox</id>
										<baseDirectory>${project.build.directory}</baseDirectory>
										<fileSets>
											<fileSet>
												<directory>classes/files</directory>
											</fileSet>
										</fileSets>
									</inline>
								</assembly>
							</build>
						</image>
						<image>
							<name>%g/dev-workstation:%l</name>
							<build>
								<contextDir>${project.basedir}/src/main/container</contextDir>
								<dockerFile>VagrantDockerfile</dockerFile>
								<filter>@</filter>
								<assembly>
									<name>maven</name>
									<targetDir>/maven</targetDir>
									<inline>
										<id>workstation</id>
										<baseDirectory>${project.build.directory}</baseDirectory>
										<fileSets>
											<fileSet>
												<directory>classes/files</directory>
											</fileSet>
										</fileSets>
									</inline>
								</assembly>
							</build>
						</image>
					</images>
				</configuration>
				<executions>
					<execution>
						<id>container-image-build</id>
						<phase>package</phase>
						<goals>
							<goal>build</goal>
						</goals>
					</execution>
					<execution>
						<id>container-image-push</id>
						<phase>deploy</phase>
						<goals>
							<goal>push</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<executions>
					<execution>
						<id>default-deploy</id>
						<phase>none</phase>
					</execution>
				</executions>
			</plugin>
			<plugin>
          <groupId>com.outbrain.swinfra</groupId>
          <artifactId>ci-friendly-flatten-maven-plugin</artifactId>
          <version>1.0.6</version>
          <executions>
            <execution>
              <goals>
                <goal>clean</goal>
                <goal>flatten</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>only-eclipse</id>
			<activation>
				<property>
					<name>m2e.version</name>
				</property>
			</activation>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<groupId>org.eclipse.m2e</groupId>
							<artifactId>lifecycle-mapping</artifactId>
							<version>${m2e.plugin.version}</version>
							<configuration>
								<lifecycleMappingMetadata>
									<pluginExecutions>
										<pluginExecution>
											<pluginExecutionFilter>
												<groupId>org.eclipse.jkube</groupId>
												<artifactId>kubernetes-maven-plugin</artifactId>
												<versionRange>[1.0.0,)</versionRange>
												<goals>
													<goal>build</goal>
													<goal>push</goal>
												</goals>
											</pluginExecutionFilter>
											<action>
												<ignore />
											</action>
										</pluginExecution>
									</pluginExecutions>
								</lifecycleMappingMetadata>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
	</profiles>
</project>
